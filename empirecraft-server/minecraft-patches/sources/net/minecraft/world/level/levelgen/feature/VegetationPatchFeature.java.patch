--- a/net/minecraft/world/level/levelgen/feature/VegetationPatchFeature.java
+++ b/net/minecraft/world/level/levelgen/feature/VegetationPatchFeature.java
@@ -73,6 +_,15 @@
                         int depth = config.depth.sample(random)
                             + (config.extraBottomBlockChance > 0.0F && random.nextFloat() < config.extraBottomBlockChance ? 1 : 0);
                         BlockPos groundPos = belowPos.immutable();
+                        // EMC start - VegetationPatchPlaceEvent
+                        if (org.bukkit.Bukkit.isPrimaryThread()) {
+                            org.bukkit.block.Block originBlock = org.bukkit.craftbukkit.block.CraftBlock.at(level.getLevel(), pos);
+                            org.bukkit.block.Block place = org.bukkit.craftbukkit.block.CraftBlock.at(level.getLevel(), groundPos);
+                            if (!(new com.empireminecraft.empirecraft.event.block.VegetationPatchPlaceEvent(place, originBlock).callEvent())) {
+                                continue;
+                            }
+                        }
+                        // EMC end - VegetationPatchPlaceEvent
                         boolean groundPlaced = this.placeGround(level, config, replaceable, random, belowPos, depth);
                         if (groundPlaced) {
                             surface.add(groundPos);
@@ -96,6 +_,15 @@
     ) {
         for (BlockPos surfacePos : surface) {
             if (config.vegetationChance > 0.0F && random.nextFloat() < config.vegetationChance) {
+                // EMC start - VegetationPatchPlaceEvent
+                if (org.bukkit.Bukkit.isPrimaryThread()) {
+                    org.bukkit.block.Block origin = org.bukkit.craftbukkit.block.CraftBlock.at(context.level().getLevel(), context.origin());
+                    org.bukkit.block.Block place = org.bukkit.craftbukkit.block.CraftBlock.at(context.level().getLevel(), surfacePos);
+                    if (!(new com.empireminecraft.empirecraft.event.block.VegetationPatchPlaceEvent(place, origin).callEvent())) {
+                        continue;
+                    }
+                }
+                // EMC end - VegetationPatchPlaceEvent
                 this.placeVegetation(level, config, context.chunkGenerator(), random, surfacePos);
             }
         }
