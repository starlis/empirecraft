--- a/net/minecraft/world/level/block/entity/HopperBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/HopperBlockEntity.java
@@ -425,6 +_,13 @@
             Direction direction = self.facing.getOpposite();
             if (isFullContainer(container, direction)) {
                 return false;
+                // EMC start - HopperFillEvent
+            }
+            com.empireminecraft.empirecraft.event.inventory.HopperFillEvent fillEvent = new com.empireminecraft.empirecraft.event.inventory.HopperFillEvent(self.getOwner(false).getInventory(), getInventory(container));
+            org.bukkit.Bukkit.getPluginManager().callEvent(fillEvent);
+            if (fillEvent.isCancelled()) {
+                return false;
+                // EMC end - HopperFillEvent
             } else {
                 return hopperPush(level, container, direction, self); // Paper - Perf: Optimize Hoppers
             }
@@ -480,6 +_,13 @@
         BlockState blockState = level.getBlockState(blockPos);
         Container container = getSourceContainer(level, hopper, blockPos, blockState);
         if (container != null) {
+            // EMC start - HopperDrainEvent
+            com.empireminecraft.empirecraft.event.inventory.HopperDrainEvent drainEvent = new com.empireminecraft.empirecraft.event.inventory.HopperDrainEvent(new org.bukkit.craftbukkit.inventory.CraftInventory(hopper), getInventory(container));
+            org.bukkit.Bukkit.getPluginManager().callEvent(drainEvent);
+            if (drainEvent.isCancelled()) {
+                return false;
+            }
+            // EMC end - HopperDrainEvent
             Direction direction = Direction.DOWN;
             skipPullModeEventFire = skipHopperEvents; // Paper - Perf: Optimize Hoppers
 
