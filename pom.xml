<project>
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.spigotmc</groupId>
    <artifactId>spigot-parent</artifactId>
    <version>dev-SNAPSHOT</version>
    <packaging>pom</packaging>

    <properties>
        <isPlugin>false</isPlugin>
        <skipTests>true</skipTests>
    </properties>

    <distributionManagement>
        <snapshotRepository>
            <uniqueVersion>false</uniqueVersion>
            <id>empireminecraft</id>
            <name>Empire Minecraft</name>
            <url>http://ci.empire.us/nexus/content/repositories/empireminecraft</url>
            <layout>default</layout>
        </snapshotRepository>
    </distributionManagement>

    <repositories>
        <repository>
            <id>empireminecraft</id>
            <url>http://ci.empire.us/nexus/content/groups/public/</url>
        </repository>
    </repositories>

    <build>
        <defaultGoal>install</defaultGoal>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.3</version>
                <executions>
                    <execution>
                        <phase>install</phase>
                        <configuration>
                            <tasks>
                                <taskdef resource="net/sf/antcontrib/antlib.xml" classpathref="maven.plugin.classpath" />
                                <if>
                                    <equals arg1="${project.artifactId}" arg2="spigot" />
                                    <then>
                                        <copy tofile="${project.build.directory}/../../../../EMC-Test-Server/minecraft_server.jar">
                                            <fileset dir="${project.build.directory}" includes="${project.artifactId}.jar" />
                                        </copy>
                                        <copy tofile="${project.build.directory}/../../../../EMC-Core/server/minecraft_server.jar">
                                            <fileset dir="${project.build.directory}" includes="${project.artifactId}.jar" />
                                        </copy>
                                    </then>
                                </if>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>ant-contrib</groupId>
                        <artifactId>ant-contrib</artifactId>
                        <version>1.0b3</version>
                        <exclusions>
                            <exclusion>
                                <groupId>ant</groupId>
                                <artifactId>ant</artifactId>
                            </exclusion>
                        </exclusions>
                    </dependency>
                    <dependency>
                        <groupId>ant</groupId>
                        <artifactId>ant-nodeps</artifactId>
                        <version>1.6.5</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>
    <modules>
        <module>EMC-Bukkit</module>
        <module>EMC-CraftBukkit</module>
    </modules>
</project>
