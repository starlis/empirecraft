From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Aikar <aikar@aikar.co>
Date: Tue, 23 Jun 2015 23:35:30 -0400
Subject: [PATCH] EMC Specific User API's

For quick access to our server user data object and user id

diff --git a/src/main/java/net/minecraft/server/level/ServerPlayer.java b/src/main/java/net/minecraft/server/level/ServerPlayer.java
index c34afef59372c8480c5c41c91c52f02498ba8d28..f9f14883dbd2e11b357ad5b95ab4730c1236fcf0 100644
--- a/src/main/java/net/minecraft/server/level/ServerPlayer.java
+++ b/src/main/java/net/minecraft/server/level/ServerPlayer.java
@@ -323,6 +323,24 @@ public class ServerPlayer extends Player {
     public double lastEntitySpawnRadiusSquared = -1.0;
     // Paper end - optimise chunk tick iteration
 
+    // EMC start
+    private long userId;
+    @Nullable
+    private Object user;
+    public long getUserId() {
+        return userId;
+    }
+    public void setUserId(long userId) {
+        this.userId = userId;
+    }
+    @Nullable
+    public <T> T getUser() {
+        return (T) user;
+    }
+    public void setUser(Object user) {
+        this.user = user;
+    }
+    // EMC end
     public ServerPlayer(MinecraftServer server, ServerLevel world, GameProfile profile, ClientInformation clientOptions) {
         super(world, world.getSharedSpawnPos(), world.getSharedSpawnAngle(), profile);
         this.chatVisibility = ChatVisiblity.FULL;
diff --git a/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java b/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java
index 714024d4e7fbc655468dc895f57a4faae695d633..0a03f867c5eff19d49bf676c47d130ce67fa2b06 100644
--- a/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java
+++ b/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java
@@ -191,6 +191,24 @@ public class CraftPlayer extends CraftHumanEntity implements Player {
     private long lastSaveTime;
     // Paper end
 
+    // EMC start
+    @Override
+    public long getUserId() {
+        return getHandle().getUserId();
+    }
+    @Override
+    public void setUserId(long userId) {
+        getHandle().setUserId(userId);
+    }
+    @Override
+    public <T> T getUser() {
+        return getHandle().getUser();
+    }
+    @Override
+    public void setUser(Object object) {
+        getHandle().setUser(object);
+    }
+    // EMC end
     public CraftPlayer(CraftServer server, ServerPlayer entity) {
         super(server, entity);
 
