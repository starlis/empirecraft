From da535666835526f427bc0ef75876d1e7d010a96e Mon Sep 17 00:00:00 2001
From: Aikar <aikar@aikar.co>
Date: Sun, 23 Jul 2017 23:14:49 -0400
Subject: [PATCH] Disable Recipe book

---
 src/main/java/net/minecraft/server/PlayerConnection.java | 6 ++++++
 1 file changed, 6 insertions(+)

diff --git a/src/main/java/net/minecraft/server/PlayerConnection.java b/src/main/java/net/minecraft/server/PlayerConnection.java
index 23efd9b2ac..e5bbba0f7a 100644
--- a/src/main/java/net/minecraft/server/PlayerConnection.java
+++ b/src/main/java/net/minecraft/server/PlayerConnection.java
@@ -2043,6 +2043,12 @@ public class PlayerConnection implements PacketListenerPlayIn, ITickable {
         PlayerConnectionUtils.ensureMainThread(packetplayinautorecipe, this, this.player.x());
         this.player.resetIdleTimer();
         if (this.player.activeContainer.windowId == packetplayinautorecipe.a() && this.player.activeContainer.c(this.player)) {
+            // EMC start - disable auto recipe
+            if (true) {
+                this.player.getBukkitEntity().sendMessage(org.bukkit.ChatColor.COLOR_CHAR + "cSorry, but this feature is currently disabled due to a vanilla bug. We will re-enable it once the bug is resolved.");
+                return;
+            }
+            // EMC end
             this.player.playerConnection.sendPacket(new PacketPlayOutTransaction(packetplayinautorecipe.a(), packetplayinautorecipe.b(), true));
             Iterator iterator;
             PacketPlayInAutoRecipe.a packetplayinautorecipe_a;
-- 
2.13.0

