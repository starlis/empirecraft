From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Aikar <aikar@aikar.co>
Date: Tue, 23 Jun 2015 23:35:30 -0400
Subject: [PATCH] EMC Specific User API's

For quick access to our server user data object and user id

diff --git a/src/main/java/net/minecraft/server/level/ServerPlayer.java b/src/main/java/net/minecraft/server/level/ServerPlayer.java
index b33e5cfd7401c800ae982e14c5483428bc8a98fe..d58d1385fd05fe778503fc42b667dbe2603c67f1 100644
--- a/src/main/java/net/minecraft/server/level/ServerPlayer.java
+++ b/src/main/java/net/minecraft/server/level/ServerPlayer.java
@@ -266,6 +266,24 @@ public class ServerPlayer extends Player {
     public final com.destroystokyo.paper.util.misc.PooledLinkedHashSets.PooledObjectLinkedOpenHashSet<ServerPlayer> cachedSingleHashSet; // Paper
     public org.bukkit.event.player.PlayerQuitEvent.QuitReason quitReason = null; // Paper - there are a lot of changes to do if we change all methods leading to the event
 
+    // EMC start
+    private long userId;
+    @Nullable
+    private Object user;
+    public long getUserId() {
+        return userId;
+    }
+    public void setUserId(long userId) {
+        this.userId = userId;
+    }
+    @Nullable
+    public <T> T getUser() {
+        return (T) user;
+    }
+    public void setUser(Object user) {
+        this.user = user;
+    }
+    // EMC end
     public ServerPlayer(MinecraftServer server, ServerLevel world, GameProfile profile, @Nullable ProfilePublicKey publicKey) {
         super(world, world.getSharedSpawnPos(), world.getSharedSpawnAngle(), profile, publicKey);
         this.chatVisibility = ChatVisiblity.FULL;
diff --git a/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java b/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java
index fc99dc44c80962ede5985c129c94060474ef929c..6de838326d8ce198d71e013e3e93215b0209989f 100644
--- a/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java
+++ b/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java
@@ -174,6 +174,24 @@ public class CraftPlayer extends CraftHumanEntity implements Player {
     private long lastSaveTime;
     // Paper end
 
+    // EMC start
+    @Override
+    public long getUserId() {
+        return getHandle().getUserId();
+    }
+    @Override
+    public void setUserId(long userId) {
+        getHandle().setUserId(userId);
+    }
+    @Override
+    public <T> T getUser() {
+        return getHandle().getUser();
+    }
+    @Override
+    public void setUser(Object object) {
+        getHandle().setUser(object);
+    }
+    // EMC end
     public CraftPlayer(CraftServer server, ServerPlayer entity) {
         super(server, entity);
 
