From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Aikar <aikar@aikar.co>
Date: Wed, 7 Aug 2013 19:52:14 -0400
Subject: [PATCH] VehicleExitEvent Dismount Reason API

---
 .../net/minecraft/server/players/PlayerList.java    |  1 +
 .../java/net/minecraft/world/entity/Entity.java     | 13 ++++++++++++-
 .../net/minecraft/world/entity/EntityLiving.java    |  2 ++
 .../minecraft/world/entity/player/EntityHuman.java  |  1 +
 4 files changed, 16 insertions(+), 1 deletion(-)

diff --git a/src/main/java/net/minecraft/server/players/PlayerList.java b/src/main/java/net/minecraft/server/players/PlayerList.java
index 2df8e914f66176e22aeddf8b94a83af5ea921d88..9b077b7511b6b1daa4c951745767c6c4428020ce 100644
--- a/src/main/java/net/minecraft/server/players/PlayerList.java
+++ b/src/main/java/net/minecraft/server/players/PlayerList.java
@@ -624,6 +624,7 @@ public abstract class PlayerList {
 
             if (entity.hasSinglePlayerPassenger()) {
                 PlayerList.LOGGER.debug("Removing player mount");
+                entityplayer.dismountReason = org.bukkit.event.vehicle.VehicleExitEvent.DismountReason.DISCONNECT; // EMC
                 entityplayer.stopRiding();
                 worldserver.removeEntity(entity);
                 entity.dead = true;
diff --git a/src/main/java/net/minecraft/world/entity/Entity.java b/src/main/java/net/minecraft/world/entity/Entity.java
index 2de268a215bcf030fe73c03d8dce5c495e6308ca..92c91be42783a656a96c7b597a98278d18be0a96 100644
--- a/src/main/java/net/minecraft/world/entity/Entity.java
+++ b/src/main/java/net/minecraft/world/entity/Entity.java
@@ -374,6 +374,7 @@ public abstract class Entity implements INamableTileEntity, ICommandListener, ne
         }
 
         if (this.isPassenger()) {
+            if (this.dead) this.dismountReason = org.bukkit.event.vehicle.VehicleExitEvent.DismountReason.DEAD; // EMC
             this.stopRiding();
         }
 
@@ -519,6 +520,7 @@ public abstract class Entity implements INamableTileEntity, ICommandListener, ne
         if (isDisabled) { return; } // EMC
         this.world.getMethodProfiler().enter("entityBaseTick");
         if (this.isPassenger() && this.getVehicle().dead) {
+            this.dismountReason = org.bukkit.event.vehicle.VehicleExitEvent.DismountReason.DEAD_VEHICLE; // EMC
             this.stopRiding();
         }
 
@@ -2117,6 +2119,7 @@ public abstract class Entity implements INamableTileEntity, ICommandListener, ne
             return false;
         } else {
             if (this.isPassenger()) {
+                entity.dismountReason = VehicleExitEvent.DismountReason.TRANSFER; // EMC
                 this.stopRiding();
             }
 
@@ -2137,7 +2140,11 @@ public abstract class Entity implements INamableTileEntity, ICommandListener, ne
 
     public void ejectPassengers() {
         for (int i = this.passengers.size() - 1; i >= 0; --i) {
-            ((Entity) this.passengers.get(i)).stopRiding();
+            // EMC start
+            Entity entity = this.passengers.get(i);
+            if (this.dead) entity.dismountReason = org.bukkit.event.vehicle.VehicleExitEvent.DismountReason.DEAD_VEHICLE;
+            entity.stopRiding();
+            // EMC end
         }
 
     }
@@ -2208,6 +2215,8 @@ public abstract class Entity implements INamableTileEntity, ICommandListener, ne
     // Paper start
     protected boolean removePassenger(Entity entity) { return removePassenger(entity, false);}
     protected boolean removePassenger(Entity entity, boolean suppressCancellation) { // CraftBukkit
+        VehicleExitEvent.DismountReason reason = entity.dismountReason; // EMC
+        entity.dismountReason = VehicleExitEvent.DismountReason.UNKNOWN; // EMC
         // Paper end
         if (entity.getVehicle() == this) {
             throw new IllegalStateException("Use x.stopRiding(y), not y.removePassenger(x)");
@@ -2220,6 +2229,7 @@ public abstract class Entity implements INamableTileEntity, ICommandListener, ne
                         (Vehicle) getBukkitEntity(),
                         (LivingEntity) entity.getBukkitEntity(), !suppressCancellation // Paper
                 );
+                event.reason = reason; // EMC
                 // Suppress during worldgen
                 if (this.valid) {
                     Bukkit.getPluginManager().callEvent(event);
@@ -2642,6 +2652,7 @@ public abstract class Entity implements INamableTileEntity, ICommandListener, ne
         return String.format(Locale.ROOT, "%s['%s'/%d, uuid='%s', l='%s', x=%.2f, y=%.2f, z=%.2f, cx=%d, cz=%d, tl=%d, v=%b, d=%b]", new Object[] { this.getClass().getSimpleName(), this.getDisplayName().getString(), Integer.valueOf(this.id), this.uniqueID.toString(), this.world == null ? "~NULL~" : this.world.toString(), Double.valueOf(this.locX()), Double.valueOf(this.locY()), Double.valueOf(this.locZ()), chunkX, chunkZ, this.ticksLived, this.valid, this.dead}); // Paper - add more information
     }
 
+    public VehicleExitEvent.DismountReason dismountReason = VehicleExitEvent.DismountReason.UNKNOWN; // EMC
     public boolean isInvulnerable(DamageSource damagesource) {
         return this.invulnerable && damagesource != DamageSource.OUT_OF_WORLD && !damagesource.v() && !isDisabled; // EMC - add && !isDisabled
     }
diff --git a/src/main/java/net/minecraft/world/entity/EntityLiving.java b/src/main/java/net/minecraft/world/entity/EntityLiving.java
index 2acdee2673c2d1e4ec34e610cb8fd0b4312d90cd..b809f0a48a1638854ae373e736e81f889517a755 100644
--- a/src/main/java/net/minecraft/world/entity/EntityLiving.java
+++ b/src/main/java/net/minecraft/world/entity/EntityLiving.java
@@ -404,6 +404,7 @@ public abstract class EntityLiving extends Entity {
                 }
 
                 if (!this.world.isClientSide && this.isPassenger() && this.getVehicle() != null && !this.getVehicle().bt()) {
+                    this.dismountReason = org.bukkit.event.vehicle.VehicleExitEvent.DismountReason.WATER; // EMC
                     this.stopRiding();
                 }
             } else if (this.getAirTicks() < this.bH()) {
@@ -3574,6 +3575,7 @@ public abstract class EntityLiving extends Entity {
 
     public void entitySleep(BlockPosition blockposition) {
         if (this.isPassenger()) {
+            if (this instanceof EntityHuman) this.dismountReason = org.bukkit.event.vehicle.VehicleExitEvent.DismountReason.PLAYER; // EMC
             this.stopRiding();
         }
 
diff --git a/src/main/java/net/minecraft/world/entity/player/EntityHuman.java b/src/main/java/net/minecraft/world/entity/player/EntityHuman.java
index 08f5b6374d91082460e9987dda583b3d650352a3..cfa0518e55b93fb3de511f30011475d665d8ffb1 100644
--- a/src/main/java/net/minecraft/world/entity/player/EntityHuman.java
+++ b/src/main/java/net/minecraft/world/entity/player/EntityHuman.java
@@ -467,6 +467,7 @@ public abstract class EntityHuman extends EntityLiving {
     @Override
     public void passengerTick() {
         if (this.er() && this.isPassenger()) {
+            this.dismountReason = org.bukkit.event.vehicle.VehicleExitEvent.DismountReason.PLAYER; // EMC
             this.stopRiding();
             this.setSneaking(false);
         } else {
